<Page x:Class="E_store.Views.EditUserPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:E_store.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Edit user">

    <StackPanel Margin="30">
        <TextBlock Text="Edit user" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

        <TextBlock Text="Inser username to search:"/>
        <DockPanel Margin="0,5,0,20">
            <Button Content="Search" Width="100" DockPanel.Dock="Right" Click="BtnSearch_Click"/>
            <TextBox x:Name="TxtSearch" Margin="0,0,10,0"/>
        </DockPanel>

        <StackPanel x:Name="PanelEdit" Visibility="Collapsed">

            <TextBlock Text="Name:"/>
            <TextBox x:Name="TxtFirstName" Margin="0,5,0,10"/>

            <TextBlock Text="Surname:"/>
            <TextBox x:Name="TxtLastName" Margin="0,5,0,10"/>

            <TextBlock Text="Username:"/>
            <TextBox x:Name="TxtUsername" Margin="0,5,0,10"/>

            <TextBlock Text="Email:"/>
            <TextBox x:Name="TxtEmail" Margin="0,5,0,10"/>

            <TextBlock Text="Address:"/>
            <TextBox x:Name="TxtAddress" Margin="0,5,0,10"/>

            <TextBlock Text="Telephone:"/>
            <TextBox x:Name="TxtPhone" Margin="0,5,0,10"/>
            <!--
            <TextBlock Text="New password (empty if :"/>
            <PasswordBox x:Name="TxtPassword" Margin="0,5,0,10"/>
-->
            <TextBlock Text="Role:" FontWeight="Bold" Margin="0,15,0,5"/>
            <ItemsControl x:Name="Roles">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding RoleName}" IsChecked="{Binding IsChecked}" Margin="0,2,0,2"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Text="Current permissions:" FontWeight="Bold" Margin="0,15,0,5"/>
            <TextBlock x:Name="LblPermissions" TextWrapping="Wrap" Foreground="#555"/>

            <Button Content="Save" Click="BtnSave_Click"
                    Height="35" Width="180" HorizontalAlignment="Left" Margin="0,15,0,0"/>
            
        </StackPanel>

        <TextBlock x:Name="LblMessage" Foreground="Red" Margin="0,15,0,0" TextWrapping="Wrap"/>
    </StackPanel>
</Page>
